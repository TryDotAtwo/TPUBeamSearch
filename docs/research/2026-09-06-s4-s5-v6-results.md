# S5 V6: explicit HBM output resolves tested wire failure

Source b2d6f8b8a19b2da1e30d658529d7b26cd437daf8, launcher42c3659.
Terminal COMPLETE, full output in test_results/beam_s4_s5_v6.
Both hbm and hbm_initialized subprocesses returned0 and passed all8cases:
zero mismatches, expected/output SHA-256 equal. Runtime JAX/jaxlib0.10.2,
libtpu0.0.42.1, eight TPU v5 lite devices IDs0..7.

Changing the custom-call output type from ShapeDtypeStruct to pltpu.HBM,
while preserving the remote body and BlockSpecs, resolves the failure in this
tested geometry. Initialization is not required: the hbm variant omits it.
The hbm HLO output loses S(1), while a root copy still exists. Therefore the
mere presence of the root copy is not itself sufficient to cause failure.
Explicit custom-call output placement, not just a kernel Ref BlockSpec, is
the supported repair direction for this transport path.

Scope: width256, two uint32 planes/rank,8ranks, repeated nonzero/zero/nonzero/
singleton sequence. This does not establish correctness for all widths,
integrated histogram reduction, coordinated threshold epochs, or full beam.
No matched performance samples were collected; compile time is not latency.
Next bundle should use explicit placement for composed histogram summation
and the prepared20 state-carrying S5 epochs without changing BN/inference
defaults. Preserve source/runtimes/HLO and correctness gates.

Artifacts are synthetic public seed603 fixture metrics and compiler/runtime
logs. No model/checkpoint/user input is loaded by the probe.
