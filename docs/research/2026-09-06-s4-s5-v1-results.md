# S4/S5 V1 physical results: S4 accepted, S5 rejected

Private `trydotatwo/tpu-beam-s4-s5-gate` V1, terminal ERROR.
Source c35734079bb117322fbaaee72fd1d4e20191eab8, launcher5ef3c7f.
All three groups ran sequentially. JAX/jaxlib0.10.2, libtpu0.0.42.1,
eight TPU v5 lite devices IDs0..7. Complete outputs/logs/HLO retained in
`test_results/beam_s4_s5_v1/`. Do not redownload or rerun successful S4 merely
because the coordinator returned failure.

S4 returned0, exact=true, expected/output SHA matched. Median0.667710 ms,
p10/p900.620680/0.693610 ms,3 warmup/21 repeats. This is the bounded reserved
S4 fixture, not whole-beam throughput or a matched A/B speedup.

Request returned1 before executable generation. The concrete rejected
expression is `jnp.max(wire[...],axis=0)` over uint32 in beam_s5_request.py;
Mosaic reports `Reductions over unsigned integers not implemented.` No request
case executed and no request timing is available. A local red structural test
reproduced the unsigned reduction; a signed-order transform repair is being
validated separately. Local passing tests cannot prove physical compilation.

Histogram compiled (0.38365148 s) but returned1. Zero case passed. The very
next mixed case has4096 mismatched words, with actual output hash identical to
the all-zero case. Remaining cases did not run. The saved HLO retains runtime
parameter -> exchange custom call -> pair-sum custom call; it does not show
replacement of the entire computation by a literal zero. Attribution between
transport, memory/layout handoff and reduction still needs isolation. Do not
claim the source cause is known or weaken correctness acceptance.

Recovery should preserve S4 acceptance and isolate raw histogram transport,
synthetic pair reduction and combined results using nonzero-first and repeated
inputs on the same executable. Request signed-MAX repair needs physical
recheck. Integrated S5 epoch must not be promoted until these primitives pass.
