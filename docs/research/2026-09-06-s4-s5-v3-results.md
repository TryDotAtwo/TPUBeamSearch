# S5 V3: own-copy passes; full wire is zero

Private gate V3 terminal ERROR, downloaded completely to
`test_results/beam_s4_s5_v3`. Source63cd100c0c0c7266acc5f2b82368b07f3d035322,
launcher196a9d1. Both processes identify eight TPU v5 lite devices0..7,
JAX/jaxlib0.10.2 and libtpu0.0.42.1.

Own-copy process returned0, exact8/8. The unchanged local tiled HBM/VMEM copy
loop works in the reduced [2,256] output configuration on all tested inputs.
Wire process returned1, first nonzero case failed with32704 mismatched words.
Later wire cases were not executed.

The retained `wire/recovery0_0_failure.npz` contains uint32 input[8,2,256],
expected[8,16,256], actual[8,16,256]. Nonzero counts are4088,32704,0.
Every actual rank/row is zero, including each rank's own two rows. This is
direct array evidence, not only matching hashes or a traceback interpretation.

This narrows but does not prove the source cause. Own-only changes both the
presence of remote transfers and the output shape from[16,256] to[2,256].
The next discriminator must keep the full output shape without remote DMA;
initialize every output region deliberately, then write the own contribution.
Use matched layout and inspect own and other rows separately. An independent
single-peer variant can distinguish many-peer sequencing once output-layout
behavior is understood. Do not call the local tile loop broken on this evidence.

No timings or beam/inference speedup are claimed. The complete S5 epoch remains
unaccepted. This run does not repeat accepted S4/request/reduction checks.
All outputs/logs/HLO and failure arrays are published; no new TPU run submitted
as part of this report.
