# S5 V4: full-shape local replication passes

Source `89b4d3c5616e5a701f32c854bb7ee66ac83b4825`, launcher `d364d4c`.
Private `trydotatwo/tpu-beam-s4-s5-gate` completed. Complete output is in
`test_results/beam_s4_s5_v4/`, including coordinator JSON, nested JSON,
process log, Kaggle log and compiled HLO.

The single replicate subprocess returned 0. All eight cases were exact:
zero mismatches and matching expected/output SHA-256. Runtime was JAX and
jaxlib 0.10.2, libtpu 0.0.42.1, eight TPU v5 lite devices (IDs 0 through 7).
The repeated nonzero/zero/nonzero/singleton sequence passed twice on the same
executable. Compile time was 0.41631744 seconds, not inference latency.

This control retains the full `[16,256]` per-device wire shape but fills all
eight pair slots with the local `[2,256]` input. It uses no remote transfer.
It therefore establishes that the full output shape and the tiled local
HBM/VMEM copy can work together. Unlike the V3 own-only control, it does not
shrink the output to `[2,256]`.

V3 remote wire still failed with all-zero output, including the own slot.
V4 narrows the investigation to differences introduced by the remote path;
it does not establish a specific faulty expression, semaphore, compiler pass
or runtime behavior. Replication also initializes all output slots whereas
remote wire initializes peer slots through incoming DMA. Initialization and
remote scheduling remain candidate distinctions to isolate.

Next diagnostic should retain full shape and initialize all slots, then add
the unchanged remote path. Compare final peer-offset identities, not just
nonzero counts. Preserve failed arrays and HLO if it fails. Do not promote a
production fix until physical evidence supports the change.

No matched timing, integrated S5, CUDA replay, beam speedup, or overlap claim
is supported by this control. Existing S4/request/reduction acceptance is not
rerun or broadened here.
