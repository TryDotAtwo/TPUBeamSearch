# S5 V8: publication dependency compile rejection

Private kernel `trydotatwo/tpu-beam-s4-s5-gate` is terminal ERROR.
Downloaded all returned artifacts to `test_results/beam_s4_s5_v8`.
Source b8e063d6fe367b0b557eb0dcf8916caddfc0af23, launcher6a0e770.
Runtime JAX/jaxlib0.10.2, libtpu0.0.42.1, eight TPU v5 lite devices IDs0..7.

Epoch subprocess returned1. Nested cases is empty; exact/all_exact false.
No epoch execution, no HLO artifact or execution timing was produced.

The compiler reports `Cannot store scalars to VMEM` for
`beam_s5_epoch.py:35`, `out[0] = active[0,0]+jnp.uint32(1)` in
`beam_s5_publication_dependency`. Unlike the earlier native layout abort,
this traceback identifies a direct unsupported scalar store expression.
The width128 threshold scan fix has not yet obtained end-to-end epoch
acceptance; getting past its earlier rejection is not numerical evidence.

Next: regression for the publication dependency store shape, replace the
scalar store with a vector store preserving the dependency and signal value,
focused/full tests and a new pinned SHA. Do not rerun V6/V7 accepted histogram
cases. Current local regression83193 is still running against unchanged Python
sources; make the fix after that snapshot terminates. Keep final batch-count
guards from the separate audit in the following checked patch scope too.
