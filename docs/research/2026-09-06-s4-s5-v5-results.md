# S5 V5 initialized remote wire still fails

Source cf66f4027af3311fa5cb7ce8a0ae6b7ded8164e3, launcher fa9391c.
Full output downloaded to `test_results/beam_s4_s5_v5` after terminal ERROR.
Runtime JAX/jaxlib0.10.2, libtpu0.0.42.1; eight TPU v5 lite IDs0..7.

First case recovery0_0 has32704 mismatches; later seven cases were not run.
Saved NPZ confirms input[8,2,256] has4088 nonzero words, expected[8,16,256]
has32704, actual[8,16,256] is entirely zero. Output SHA matches V3's zero
result. This is a correctness failure, not a native compiler abort.

The added initialization fills all output slots locally before the unchanged
remote protocol. It does not repair the failure. V4 established the same
full-shape local replication works without communication. Therefore missing
local initialization alone does not explain the observed remote result.
This still does not identify a specific failing compiler/runtime expression.

Next investigation: inspect communicating output memory assignment/root copy
against local control, and design an explicitly initialized aliased output
control or separate output copy boundary. Preserve peer-offset expected
identities and all failed arrays. Do not change production defaults based on
this rejected diagnostic. No timing or integrated S5 acceptance is claimed.

Publication audit: the NPZ was opened with allow_pickle=False and its only
members are input/expected/actual. Input and expected are elementwise identical
to recovery_fixtures('initialized')[0], generated from the public seed603
synthetic histogram code. Actual is exclusively uint32 zeros with expected
shape. Both complete logs were read: only runtime warnings, source checkout,
group name, and traceback are present; no credentials or user/model data.
