# S5 recovery V2: failure isolated before pair reduction

Private `trydotatwo/tpu-beam-s4-s5-gate` V2 terminal ERROR. Complete downloaded
output is `test_results/beam_s4_s5_v2`, including bundle, four process logs,
four nested JSON reports, four HLO files and Kaggle log.
Source `03823936c339a7a57305474e35f938b3b0a556ef`, launcher `b0d60f7`.
All four reports identify JAX/jaxlib0.10.2, libtpu0.0.42.1 and eight TPU v5 lite
devices with IDs0..7. Each group ran in a separate process, sequentially.

| Group | Return code | Exact | Executed cases | First mismatch words |
|---|---:|---|---:|---:|
| request | 0 | yes | 20/20 | 0 |
| wire | 1 | no | 1/8 | 32704 |
| reduction | 0 | yes | 8/8 | 0 |
| combined | 1 | no | 1/8 | 4096 |

The signed-order MAX repair passes the existing physical request corpus.
Synthetic pair reduction passes nonzero/zero/nonzero/singleton twice using
one executable. This is not acceptance of all possible uint32 request inputs
or a complete S5 epoch.

Raw wire fails on the FIRST nonzero input, excluding a necessary preceding
zero invocation as the explanation. Its output SHA256
`fa43239bcee7b97ca62f007cc68487560a39e19f74f3dde7486db3f98df8e471`
matches the known all-zero wire fixture SHA in the successful reduction
corpus. Expected wire SHA is
`ce7459f2d07c131671f0ff29f12c924cc1cc3846dd0ae8beb57a58e5366276f1`,
which is exactly the first synthetic reduction input SHA. Reduction of that
input produces expected global sum SHA
`a9639f9a9aed1335e3f9bb9c60f4dfa26aaeee32b70dd84e4cb32b9dd831aac8`.

Thus the tested failure is already in the exchange path, before pair sum.
This does NOT yet distinguish local copy, remote DMA, layout, or lowering.
Both own-rank and remote output regions are implicated by the zero output;
do not attribute it solely to network synchronization without further probes.
The wire HLO still has a runtime input and eight partitions. HLO presence
alone cannot establish that every intended copy was executed correctly.

No inference/beam speed or overlap claim. These are correctness probes, not
matched timing experiments; compile durations are not inference latency.
Accepted S4 was not rerun. Integrated S5 epoch remains blocked on transport
acceptance, while unrelated final-stage implementation can continue.

Next diagnostic must isolate own-copy-only from remote copies and retain
nonzero input/output evidence. Any production fix requires a reproducible
test, full local regression and a new pinned physical run; V2 is not restarted.
