# S5 V9: serialized physical epoch gate accepted

Private Kaggle `trydotatwo/tpu-beam-s4-s5-gate` V9 completed successfully.
Source `f9a8bee57b8d3bd075d78ff2d37cab2fdd300b94`, launcher `8bf352d`.
Downloaded output: `test_results/beam_s4_s5_v9/`, including full Kaggle log,
subprocess log, nested JSON, coordinator JSON and compiled HLO.

Runtime matches the pin: JAX/jaxlib0.10.2, libtpu0.0.42.1; eight TPU v5 lite
devices with IDs0..7. The epoch subprocess returned0. All20 unique indices
0..19 have four zero mismatch counts and identical expected/output SHA256.
Both nested `exact` and coordinator `all_exact` are true.

The probe passes actual device outputs into the next epoch. It tests two
cycles of no request, per-rank requests and all-rank requests, with alternating
A/B publication buffers and carried epoch state. Histogram input is frozen.
This accepts serialized S5 state-carry behavior, not concurrent histogram
writer/reader visibility, a full beam depth, or the complete scratch protocol.

V8 failed compilation at a scalar VMEM publication store. V9 uses a vector
store at that dependency. Physical compilation and these20 executions now
pass. Compile duration0.740424s is not inference or beam latency. No matched
timing samples or overlap profile were collected in this correctness gate.

Next physical gate is final materialization/exchange/coverage. Its pending
seven-case coverage extension must first complete local regression and be
published; update the prepared launcher's source pin before submitting it.
Do not resubmit the accepted S5 V9 gate.
