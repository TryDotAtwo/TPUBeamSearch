# S4/S5 V7: histogram sum accepted, epoch lowering rejected

Source `6c3fc5936616b7c22bf502030477eead523c356b`, launcher `e68300f`.
Kaggle terminal ERROR. Artifacts: `test_results/beam_s4_s5_v7`.
Both processes report JAX/jaxlib0.10.2, libtpu0.0.42.1 and eight TPU v5 lite
devices, IDs0..7. Source hashes match the pinned launch.

| Group | Return code | Result |
|---|---:|---|
| hbm_combined | 0 | 8/8 exact, zero mismatches, output SHA equals expected in every case |
| epoch | 1 | compilation failure; zero epochs executed |

The explicit-HBM wire plus pair-word sum now has physical correctness evidence
at width256, including nonzero/zero/repeated/single-source and carry fixtures.
Compile time0.476s is not execution latency; no throughput timing was collected.
The complete S5 epoch is not accepted and bundle all_exact is false.

## Epoch failure and local reproduction

The exception is a recoverable MLIR lowering error, not a native process abort:
`vector types must have positive constant sizes but got 0`. Source location is
`beam_threshold.py:30`, the carry-aware tuple `lax.associative_scan`.

A minimal local JAXPR of the same uint32 low/high scan contains four zero-sized
slice outputs and subsequent zero-sized arithmetic. The regression test walks
the actual threshold Pallas JAXPR and fails on `slice -> uint32[0]`. This gives
an expression-level mechanism consistent with the TPU lowering error; local
JAXPR alone is not a successful TPU compile or epoch execution.

The local candidate replaces only that scan with seven width128 shift/add
stages, distances1,2,4,8,16,32,64. Each stage retains low-word wrap and carry
into high. No threshold criterion, histogram population, communication or
publication rule is relaxed. Nine local scan/threshold/epoch tests pass17.08s.
Physical recompilation and twenty state-carry epochs remain pending.

Do not rerun the accepted combined group solely to fill the next bundle. The
next physical gate should isolate the corrected epoch, retaining full partial
logs and source/runtime checks. Other final-transport experiments are independent
and must not be described as fixes for this threshold lowering issue.
